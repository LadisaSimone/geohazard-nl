"""PDF export of the dashboard's hazard reports.

TODO(claude-code): implement
- build_pdf(province_reports: dict[str, dict]) -> bytes
  Uses reportlab. One section per province: hazard table + narrative text.
"""
